{
  "install": "npm --prefix apps/web ci",
  "steps": [
    { "name": "typecheck", "run": "npm --prefix apps/web run typecheck" },
    { "name": "test", "run": "npm --prefix apps/web test" },
    { "name": "build", "run": "npm --prefix apps/web run build" }
  ],
  "watch": ["main"]
}
